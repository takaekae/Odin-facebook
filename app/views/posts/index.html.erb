
<%= render "new_post_form" %>

<!-- shows an existing post -->
<div class="section">
  <div class="container">
    <div class="content">
      <h1>Timeline</h1>
      <% @posts.each do |post| %>
        <%= render "post_media", post: post %>
      <% end %>
    </div>
  </div>
</div>
